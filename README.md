# ConversorDec2Bin
# Este programa funciona para convertir de decimal a binario 
# El binario resultante es un string e.g. "101"
# Se usa la función bin() que transforma e.g. 3 en "0b11"
# En esta función se quita el "0b" para dejar el "11"